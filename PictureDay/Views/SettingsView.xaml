<UserControl x:Class="PictureDay.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Privacy Filter Section -->
        <GroupBox Grid.Row="0" Header="Privacy Filter - Blocked Applications" Margin="0,0,0,10">
            <Grid Margin="10">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Text="When these applications are running, screenshots will be blocked."
                           Margin="0,0,0,10" TextWrapping="Wrap"/>

                <ListBox x:Name="BlockedAppsListBox" Grid.Row="1" Height="150" Margin="0,0,0,10"/>

                <StackPanel Grid.Row="2" Orientation="Horizontal">
                    <TextBox x:Name="NewAppTextBox" Width="200" Height="25" Margin="0,0,5,0"
                             VerticalContentAlignment="Center"/>
                    <Button Content="Add Application" Width="120" Height="25" Margin="5,0,5,0"
                            Click="AddAppButton_Click"/>
                    <Button Content="Remove Selected" Width="120" Height="25" Margin="5,0,5,0"
                            Click="RemoveAppButton_Click"/>
                    <Button Content="Browse Running Processes" Width="180" Height="25" Margin="5,0,0,0"
                            Click="BrowseProcessesButton_Click"/>
                </StackPanel>
            </Grid>
        </GroupBox>

        <!-- Start with Windows -->
        <GroupBox Grid.Row="1" Header="Startup Options" Margin="0,0,0,10">
            <StackPanel Margin="10">
                <CheckBox x:Name="StartWithWindowsCheckBox" Content="Start with Windows"
                          Margin="0,5"/>
            </StackPanel>
        </GroupBox>

        <!-- Screenshot Directory -->
        <GroupBox Grid.Row="2" Header="Screenshot Directory" Margin="0,0,0,10">
            <Grid Margin="10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBox x:Name="DirectoryTextBox" Grid.Column="0" Height="25"
                         IsReadOnly="True" VerticalContentAlignment="Center" Margin="0,0,5,0"/>
                <Button Grid.Column="1" Content="Browse..." Width="80" Height="25"
                        Click="BrowseDirectoryButton_Click"/>
            </Grid>
        </GroupBox>

        <!-- Buttons -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="Save" Width="80" Height="30" Margin="5,0" Click="SaveButton_Click"/>
            <Button Content="Cancel" Width="80" Height="30" Margin="5,0" Click="CancelButton_Click"/>
        </StackPanel>
    </Grid>
</UserControl>

