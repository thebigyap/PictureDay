<UserControl x:Class="PictureDay.Views.PhotoGalleryView"
			 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
			 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
			 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
			 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
			 xmlns:local="clr-namespace:PictureDay.Views"
			 mc:Ignorable="d"
			 d:DesignHeight="450" d:DesignWidth="800">
	<UserControl.Resources>
		<BooleanToVisibilityConverter x:Key="BoolToVis"/>
		<local:InverseBooleanToVisibilityConverter x:Key="InverseBoolToVis"/>

		<!-- Stat card -->
		<Style x:Key="StatCard" TargetType="Border">
			<Setter Property="Background" Value="{DynamicResource CardBackgroundBrush}"/>
			<Setter Property="CornerRadius" Value="14"/>
			<Setter Property="BorderBrush" Value="{DynamicResource SeparatorBrush}"/>
			<Setter Property="BorderThickness" Value="1"/>
			<Setter Property="Padding" Value="18,16"/>
		</Style>

		<!-- Gallery thumbnail card with hover lift -->
		<Style x:Key="GalleryCard" TargetType="Border">
			<Setter Property="Background" Value="{DynamicResource CardBackgroundBrush}"/>
			<Setter Property="CornerRadius" Value="14"/>
			<Setter Property="BorderBrush" Value="{DynamicResource SeparatorBrush}"/>
			<Setter Property="BorderThickness" Value="1"/>
			<Setter Property="Margin" Value="8"/>
			<Setter Property="Padding" Value="8"/>
			<Setter Property="Cursor" Value="Hand"/>
			<Style.Triggers>
				<Trigger Property="IsMouseOver" Value="True">
					<Setter Property="BorderBrush" Value="{DynamicResource AccentBrush}"/>
					<Setter Property="Effect">
						<Setter.Value>
							<DropShadowEffect Color="{DynamicResource ShadowColor}" BlurRadius="20" ShadowDepth="3" Opacity="0.22"/>
						</Setter.Value>
					</Setter>
				</Trigger>
			</Style.Triggers>
		</Style>
	</UserControl.Resources>

	<Grid Background="Transparent">
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="*"/>
		</Grid.RowDefinitions>

		<!-- Header: title + month navigation -->
		<Grid Grid.Row="0" Margin="28,8,28,18">
			<StackPanel VerticalAlignment="Center">
				<TextBlock Text="Gallery" Style="{StaticResource LargeTitleText}"/>
				<TextBlock Text="A snapshot of your computer, one day at a time"
						   Style="{StaticResource CaptionText}" Margin="0,2,0,0"/>
			</StackPanel>
			<StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
				<Button x:Name="PrevMonthButton" Style="{StaticResource RoundIconButton}"
						Content="&#x2039;" Margin="0,0,8,0" Click="PrevMonthButton_Click"/>
				<ComboBox x:Name="MonthComboBox" MinWidth="128" Margin="0,0,8,0"
						  SelectionChanged="MonthComboBox_SelectionChanged" DisplayMemberPath="MonthName"/>
				<ComboBox x:Name="YearComboBox" MinWidth="84" Margin="0,0,8,0"
						  SelectionChanged="YearComboBox_SelectionChanged"/>
				<Button x:Name="NextMonthButton" Style="{StaticResource RoundIconButton}"
						Content="&#x203A;" Click="NextMonthButton_Click"/>
			</StackPanel>
		</Grid>

		<!-- Stats -->
		<Grid Grid.Row="1" Margin="28,0,28,8">
			<Grid.ColumnDefinitions>
				<ColumnDefinition Width="*"/>
				<ColumnDefinition Width="*"/>
				<ColumnDefinition Width="*"/>
				<ColumnDefinition Width="*"/>
			</Grid.ColumnDefinitions>

			<Border Grid.Column="0" Style="{StaticResource StatCard}" Margin="0,0,6,0">
				<StackPanel>
					<TextBlock Text="TOTAL PHOTOS" Style="{StaticResource SectionHeaderText}"/>
					<TextBlock x:Name="TotalPhotosValue" Text="0" FontSize="30" FontWeight="Bold"
							   Foreground="{DynamicResource AccentBrush}" Margin="0,6,0,0"/>
				</StackPanel>
			</Border>

			<Border Grid.Column="1" Style="{StaticResource StatCard}" Margin="6,0,6,0">
				<StackPanel>
					<TextBlock Text="STORAGE USED" Style="{StaticResource SectionHeaderText}"/>
					<TextBlock x:Name="StorageUsedValue" Text="0 B" FontSize="30" FontWeight="Bold"
							   Foreground="{DynamicResource TextBrush}" Margin="0,6,0,0" TextTrimming="CharacterEllipsis"/>
				</StackPanel>
			</Border>

			<Border Grid.Column="2" Style="{StaticResource StatCard}" Margin="6,0,6,0">
				<StackPanel>
					<TextBlock Text="LONGEST STREAK" Style="{StaticResource SectionHeaderText}"/>
					<TextBlock x:Name="LongestStreakValue" Text="0 days" FontSize="30" FontWeight="Bold"
							   Foreground="{DynamicResource TextBrush}" Margin="0,6,0,0" TextTrimming="CharacterEllipsis"/>
				</StackPanel>
			</Border>

			<Border Grid.Column="3" Style="{StaticResource StatCard}" Margin="6,0,0,0">
				<StackPanel>
					<TextBlock Text="CURRENT STREAK" Style="{StaticResource SectionHeaderText}"/>
					<TextBlock x:Name="CurrentStreakValue" Text="0 days" FontSize="30" FontWeight="Bold"
							   Foreground="{DynamicResource TextBrush}" Margin="0,6,0,0" TextTrimming="CharacterEllipsis"/>
				</StackPanel>
			</Border>
		</Grid>

		<!-- Photo grid -->
		<ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled"
					  Padding="20,8,20,16">
			<ItemsControl x:Name="PhotosItemsControl">
				<ItemsControl.ItemsPanel>
					<ItemsPanelTemplate>
						<UniformGrid Columns="3"/>
					</ItemsPanelTemplate>
				</ItemsControl.ItemsPanel>
				<ItemsControl.ItemTemplate>
					<DataTemplate>
						<Border Style="{StaticResource GalleryCard}">
							<StackPanel>
								<Grid Height="160">
									<Image Source="{Binding ImageSource}"
										   Stretch="Uniform"
										   RenderOptions.BitmapScalingMode="HighQuality"
										   MouseLeftButtonDown="Image_MouseLeftButtonDown"
										   MouseRightButtonDown="Image_MouseRightButtonDown"
										   Cursor="Hand"
										   Visibility="{Binding IsNoPhotos, Converter={StaticResource InverseBoolToVis}}">
										<Image.ContextMenu>
											<ContextMenu>
												<MenuItem Header="Open" Click="ContextMenu_Open_Click"/>
												<MenuItem Header="Open in File Explorer" Click="ContextMenu_OpenInExplorer_Click"/>
												<MenuItem Header="Copy Image" Click="ContextMenu_CopyImage_Click"/>
												<MenuItem Header="Copy File Location" Click="ContextMenu_CopyPath_Click"/>
												<Separator/>
												<MenuItem Header="Delete" Click="ContextMenu_Delete_Click"/>
											</ContextMenu>
										</Image.ContextMenu>
									</Image>
									<TextBlock Text="{Binding Message}"
											   FontSize="16"
											   TextAlignment="Center"
											   TextWrapping="Wrap"
											   HorizontalAlignment="Center"
											   VerticalAlignment="Center"
											   Foreground="{DynamicResource TextSecondaryBrush}"
											   Visibility="{Binding IsNoPhotos, Converter={StaticResource BoolToVis}}"/>
								</Grid>
								<TextBlock Text="{Binding DateLabel}"
										   HorizontalAlignment="Center"
										   Margin="0,10,0,2"
										   FontSize="12"
										   FontWeight="Medium"
										   Foreground="{DynamicResource TextSecondaryBrush}"
										   Visibility="{Binding IsNoPhotos, Converter={StaticResource InverseBoolToVis}}"/>
							</StackPanel>
						</Border>
					</DataTemplate>
				</ItemsControl.ItemTemplate>
			</ItemsControl>
		</ScrollViewer>
	</Grid>
</UserControl>
