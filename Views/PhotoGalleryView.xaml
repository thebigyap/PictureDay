<UserControl x:Class="PictureDay.Views.PhotoGalleryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Navigation Controls -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="10" HorizontalAlignment="Center">
            <Button x:Name="PrevMonthButton" Content="&lt;" Width="40" Height="30" Margin="5" Click="PrevMonthButton_Click"/>
            <ComboBox x:Name="MonthComboBox" Width="120" Height="30" Margin="5" SelectionChanged="MonthComboBox_SelectionChanged"/>
            <ComboBox x:Name="YearComboBox" Width="80" Height="30" Margin="5" SelectionChanged="YearComboBox_SelectionChanged"/>
            <Button x:Name="NextMonthButton" Content="&gt;" Width="40" Height="30" Margin="5" Click="NextMonthButton_Click"/>
        </StackPanel>

        <!-- Photo Gallery -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
            <ItemsControl x:Name="PhotosItemsControl" Margin="10">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <UniformGrid Columns="4" HorizontalAlignment="Center"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border BorderBrush="LightGray" BorderThickness="1" Margin="5" Padding="5">
                            <StackPanel>
                                <Image Source="{Binding ImageSource}"
                                       Width="150"
                                       Height="100"
                                       Stretch="Uniform"
                                       MouseLeftButtonDown="Image_MouseLeftButtonDown"
                                       Cursor="Hand"/>
                                <TextBlock Text="{Binding DateLabel}"
                                           HorizontalAlignment="Center"
                                           Margin="0,5,0,0"
                                           FontSize="10"/>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>

